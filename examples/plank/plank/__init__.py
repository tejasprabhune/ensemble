"""Plank: a worked example world for Ensemble.

Importing this package is enough to make `ensemble.World("plank")` work
in the current MVP; the world itself is registered in Rust at module
load time inside the ensemble extension. Persona TOMLs live under
`personas/` and scenarios under `scenarios/`.
"""

__all__: list[str] = []
