# Ensemble

Ensemble is a private framework for building multi-user, multi-agent RL
environments. Each world is a typed simulation with its own state, tools,
personas, and graders. The repo ships the framework crates, Python
bindings, a CLI, a post-training pipeline, and one worked example world
called Plank.
